#include <SFML/Graphics.hpp>
#include <cstring>
using namespace std;
char* setstr(char*& str) {
    /*int size = 0;
    while (size < strlen(str) && str[size] != '\0') ++size;
    char* newstr = new char[size + 1];
    for(int i = 0; i < size; i++) {
        newstr[i] = str[i];
    }
    newstr[size] = '\0';
    str = newstr;
    //delete[] newstr;
    return str;*/
}